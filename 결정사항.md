# 원주 수학과학축전 스탬프앱 — 결정사항 정리

2026-09-15 기준. 세부 기능·변경 이력은 `기능정리.md`, 이 문서는 **"왜 이렇게 했나"만** 모음.
행사 2026-10-10(토) 10:00–16:00 원주종합운동장 · 주최 주최측 · 부스 95개 · 하루 최대 5만 명(참여자 3~4만 가정).

---

## 1. 큰 틀

| # | 결정 | 이유 | 기각한 대안 |
|---|---|---|---|
| 1 | **QR 은 부스에 붙이고 방문객이 찍는다** | 부스에 도장 찍어줄 인력이 없음 | 부스 직원이 방문객 폰을 찍기 / 종이 스탬프 |
| 2 | **단일 HTML + Supabase REST + Vercel 정적** | 예산 0, 담당자 비개발자, 파일 하나만 배포. 서버 코드 없음 | React/빌드 도구, 별도 백엔드 |
| 3 | **로그인·Auth 없음, 익명 키 하나** | 방문객이 수천 명이라 가입 불가. MAU 도 0 이 됨 | Supabase Auth 익명 로그인 (MAU 폭증 위험) |
| 4 | **완주 = 7개**, 완주 → 설문 → 교환권 → 운영본부 수령 | 주최측 피드백(2026-09-09), 처음 5개에서 변경 | — |
| 5 | **호스팅 Vercel** (https://your-app.vercel.app) | 최신 도구 선호, 정적이라 무료 | Netlify(거절), GitHub Pages |
| 6 | **v3 채택, v2 폐기** | v3 = v2 + 설문·대시보드·최적화, 빠진 기능 없음 | v2 그대로 제출 |

## 2. 데이터·보안

| # | 결정 | 이유 |
|---|---|---|
| 7 | **등록 정보 = 학교·구분(초/중/고/성인)·학년·이름·성별만**, 연락처 없음 | 미성년자 개인정보 최소 수집. 성인은 학교 → 소속(선택) |
| 8 | **방문객 = uuid 아는 폰이 본인**. 폰 바뀌면 6자리 코드 + 이름으로 이어받기 | 카톡 인앱 ↔ 사파리, 폰 교체 대응. uuid 는 추측 불가 |
| 9 | **개인정보 테이블은 RLS 정책 없음 = RPC 로만** | 초기엔 익명 키로 명단이 읽혔음(2026-09-14 발견) → 정책 전부 제거, 뷰 revoke |
| 10 | **관리자 = 비밀번호 하나**(pgcrypto 해시, 8자 이상), 모든 관리자 RPC 에 비번 동봉. **틀리면 지연 없이 즉시 거절, 로그인 10분 안 10회 실패 → 1분 잠금** | Auth 없이 운영. 관리자 여러 명이 각자 폰으로 동시 로그인 가능. 진입은 `#/admin` 직접 입력(버튼 없음). 처음엔 틀릴 때 `pg_sleep(0.5)` 였는데 연결을 붙들어 오히려 DoS 통로(외부 조언 09-15) → 실패 횟수 표(`admin_login_fail`)로 교체. BAD_TOKEN·이어받기 실패의 0.3초 지연도 같이 제거 |
| 11 | **치팅 방지 = QR 토큰 + 도장 간격 90초**, 서버(`add_stamp` RPC)가 검사 | 주소창에 번호만 쳐도 찍히던 문제. 토큰은 한 번 발급되면 고정(인쇄 QR 유지). 링크 공유는 못 막지만 7개에 10분 넘게 걸려 이득 없음 |
| 12 | **통신 실패·8초 타임아웃은 오프라인 큐**, 서버 거절(BAD_TOKEN·TOO_FAST·NOT_DONE)은 안내 화면 | 운동장 통신 불안정. 재전송에서 dup 오면 서버 목록으로 맞춤 |
| 13 | **DB 컬럼은 풀네임**(`organization`, `sort_order`…), 앱 내부는 짧은 이름 | 담당자가 Supabase 표를 직접 볼 수 있게. 변환은 `_b()` 한 곳 |
| 14 | 행사 후 관리자 › 데이터에서 **개인정보 파기**, 그 전에 CSV 4종 export | 무료 플랜은 백업 없음 |

## 3. 설문

| # | 결정 | 이유 | 기각 |
|---|---|---|---|
| 15 | **앱 안에서 설문** (기본 7문항, 관리자가 편집) | 구글폼 안 쓰기로 함. 제출 즉시 교환권, 응답은 uuid 로만 저장(이름 없음), 서버가 완주 확인 | 구글폼 링크 + 자기신고 |
| 16 | 외부 링크 모드도 남김(60초 타이머·코드 prefill·`?survey` 복귀·탭 동기화) | 나중에 구글폼으로 바꿔도 되게 | — |

## 4. 운영

| # | 결정 | 이유 |
|---|---|---|
| 17 | **운영 대시보드는 별도 페이지**(`/dashboard`), 15초 갱신, RPC 1개 | TV 용. 관리자 현황 탭은 "숫자 확인", 대시보드는 "실시간 모니터링". 부스 전체 표(정렬·검색·30분 무도장 빨강) 포함, '조용한 부스' 카드는 뺌 |
| 18a | **부스별 참여자 명단**: 관리자 › 현황 부스 줄 / 대시보드 부스 표 줄 클릭 → 시각·이름·학교·학년·성별 | 운영자가 "이 부스 누가 왔나" 확인 요청(09-15). RPC `admin_booth_visitors` 하나, 비번 동봉 |
| 18 | **선물 수령 = 관리자 폰으로 교환권 QR 연속 스캔** (+ 6자리 코드 검색) | 줄 서면 타이핑이 느림. 교환권 QR 은 `WJ:uuid` 텍스트라 남이 찍어도 아무 일 없음 |
| 19 | **부스 편집: 엑셀(xlsx) 양식 내려받기/불러오기**, 분류는 글자 입력(없는 이름은 새 분류 자동), 구역만 드롭다운 | CSV 는 보기 힘듦. 드롭다운 싫어함. 붙여넣기 기능은 만들었다가 뺌 |
| 20 | **QR 인쇄 시트 = 세로 A4 위아래 2장(가로형 반 장)**, QR 94mm 흑백, 분류 색 띠·번호 배지 44mm, 행사명만 | 잘라서 눕혀 놓는 용도. 둥근 점 QR 은 별로라 되돌림. `@page margin 0` 으로 브라우저 머리글 제거 |
| 21 | 긴급 공지·행사 설정은 서버 settings 한 행, 5분 폴링 | 당일 변경(우천 등)이 열린 폰에 반영되게 |

## 5. 비용·한도 (3~4만 명 기준)

| # | 결정 | 근거 |
|---|---|---|
| 22 | **무료 플랜으로 간다**, Pro 는 보험 | 폭주 테스트 800명 동시 에러 0 · 지속 테스트 1차 초당 4명×10분 에러 0·p95 64ms · **2차(09-15) 초당 6명 도착×15분, 방문객 5,242명·요청 35,314건·에러 0·p95 73ms**. 초당 59건까지 평평, 65건에서 쓰기 p95 3~4초로 줄 섬(에러 없음) = 무료 컴퓨트 한계 ≈ 초당 60건 쓰기. 실제 행사는 부스 체류가 길어 도장이 초당 10~15건이라 여유 있음 |
| 23 | **js 는 CDN 우선(3초 타임아웃 → 로컬), 1년 immutable + `?v=배포시각`**, HTML 만 no-cache, favicon 인라인 | Vercel Edge Requests 100만/30일. 재방문 = 1건. 첫 방문도 1건 |
| 24 | **부스 목록은 배포 시 HTML 에 박고 버전 매칭**, 같으면 서버 요청 0 | Supabase egress 5GB/30일. 부스 목록(원본 30KB)이 1인 전송량의 절반이었음 |
| 25 | 4만 명 추정: Vercel 40만/100만, egress 1.5GB/5GB, DB 80MB/500MB, MAU 0 | 실측 기반 (1인 앱 열기 10회 가정) |
| 26 | **Pro 를 켠다면 행사 2~3일 전**, 컴퓨트 Micro 수동 변경(2분 다운타임) | 월 $25 최소. 개막 순간 폭주·7일 미사용 정지·백업 보험 |
| 27 | 무료 유지 시: 행사 전 3일 안에 앱 한 번 열기(7일 정지 방지), 행사 전날 테스트 데이터 "전부 삭제" | — |

## 6. 개발·운영 규칙

| # | 결정 |
|---|---|
| 28 | **배포는 반드시 `배포.ps1`** — testStampInput false, `?v=` 버전 치환, 부스 시드 박기, Vercel 업로드. 다른 방법으로 올리면 부스 시드가 안 박힘 |
| 29 | 실서버 테스트 뒤 `admin_reset`(전체 삭제) 금지. 테스트 데이터는 접두어 붙여 남기고 사용자가 지움 |
| 30 | 공개 깃헙(https://github.com/vittroi384/wonju-stamp-rally)은 `공개저장소_내보내기.py` 로 만든 정제본만 — Supabase 주소·키 자리표시자, 부스·기관·학교 실명은 더미, 주최·회사명 제거 |
| 31 | 실운영본은 `C:\dev\stamp-rally-v3`(git 없음), 공개본은 `C:\dev\wonju-stamp-rally`(git 루트) |
| 32 | SQL 은 `supabase_setup.sql` 전체를 SQL Editor 에 붙여 Run(재실행 안전). 현재 1~12절(11 비번 잠금 · 12 부스 참여자 명단) |

## 7. 남은 것

- 관리자 비밀번호 1234 → 8자 이상 (관리자 › 데이터)
- 부스 설명 표 받으면 엑셀 양식으로 넣고 → `배포.ps1` 한 번
- QR 시트 인쇄 (배포 주소 기준, 주소 바뀌면 재인쇄)
- Pro 켤지 결정 (행사 2~3일 전)
- 행사 전날 테스트 데이터 삭제, 행사 후 CSV export + 개인정보 파기
