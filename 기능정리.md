# 2026 원주 수학과학축전 · 부스 스탬프 웹앱 — 기능 정리

파일: `원주축전_스탬프앱_3.html` + `qrcode.min.js` + `jsQR.js` (같이 배포. 외부 의존은 Pretendard 폰트뿐, 라이브러리 없으면 CDN 폴백) + `운영대시보드.html`(운영본부 TV용, 배포 시 `dashboard.html`)
폴더: `C:\dev\stamp-rally-v3` — 실제 운영본(실명·실제 키). 공개 깃헙(https://github.com/vittroi384/wonju-stamp-rally)은 `공개저장소_내보내기.py` 로 만든 정제본 `C:\dev\wonju-stamp-rally`(더미 부스·자리표시자 키)이고 git 루트도 거기. v2(`stamp-rally`)는 v3 확정 후 불필요
배치 기준: `2026 원주수학과학축전 부스배치도-복사.pdf` (2026-08-24 수정본, 최종 아님) · 사진 `KakaoTalk_20260907_220730991.png`
행사: 2026. 10. 10. (토) 10:00–16:00 · 원주종합운동장
최종 수정: 2026-09-15

---

## 1. 전체 구조

| 구분 | 내용 |
|---|---|
| 스택 | 단일 HTML + Supabase REST + Vercel(정적 호스팅). **배포 주소 https://your-app.vercel.app** (대시보드 /dashboard), 2026-09-15 첫 배포. 재배포는 `.\배포.ps1` |
| 배포 | Vercel에 HTML 하나 올리면 끝. 깃헙은 개인 포폴용이라 배포와 무관 |
| 저장 | `CONFIG.supabaseUrl` 비면 브라우저 로컬(localStorage·IndexedDB), 채우면 Supabase |
| 사용자 | 방문객은 모바일, 관리자는 PC 브라우저 (같은 파일, `#/admin`) |
| 규모 목표 | 동접 1~3천, 하루 총 최대 5만 명. 집계는 DB 뷰만 읽어서 원본 테이블 안 긁음 |
| 부스 | PDF 기준 82 + 추가 자리 83~90 + 운영·네컷 5 = 95개 시드 (주최측 '텐트 90개'). UI는 100개까지 확인 (`CONFIG.boothMax`) |

### 화면 (해시 라우트)

| 경로 | 화면 |
|---|---|
| `#/` | 홈: 스탬프 카드, 다음 일정, 부스 검색·분류 필터·목록 |
| `#/map`, `#/map/번호` | 지도 (핀치줌·드래그), 번호 주면 그 부스 강조 + 정문 경로 |
| `#/booth/번호` | 부스 소개 (사진·설명·위치·유튜브·PDF·도장 상태) |
| `#/stamp/번호` | QR 딥링크 도착 화면. 미등록이면 등록 후 복귀 |
| `#/my` | 내 스탬프. 완주(7개) 시 설문 안내 → 앱 내 설문 제출(또는 외부 링크 + '설문 다 했어요') → 교환권(QR + 6자리 코드) |
| `#/survey` | 앱 내 설문 (완주자만). 문항은 관리자 › 설문 |
| `#/info` | 일정·안내 자료 링크 |
| `#/register` | 학교 + 구분(초·중·고·성인) + 학년 + 이름 + 성별 + 개인정보 동의 1줄 |
| `#/admin/{dash\|gift\|booths\|qr\|event\|survey\|data}` | 관리자 (비번, 기본 1234 → 8자 이상으로 변경. Supabase 모드는 서버에서 검사). 방문객 화면에 진입 버튼 없음, 주소 직접 입력(`CONFIG.adminPath`) |

QR 형식: `주소?b=부스번호&t=토큰` → `#/stamp/부스번호` (토큰은 세션에 보관, 서버가 검사). 입장용 `주소?join`. 외부 설문 복귀용 `주소?survey`.

---

## 2. 방문객 기능

- **등록**: 학교 → 구분(초등·중등·고등·성인 버튼) → 학년(초 1~6, 중·고 1~3 버튼, 성인은 없음) → 이름 → 성별(남·여). 성인은 학교 칸이 '소속(선택)'으로 바뀌고 비워도 됨. 저장은 `grade`='초3'·'중1'·'성인', `gender`='남'·'여'. 연락처 없음. 폰에 기억(재방문 시 자동 로그인). 도장 찍을 때는 아무것도 입력 안 함(등록 정보 자동).
- **도장**: 앱 내 카메라 스캐너(jsQR) 또는 폰 카메라로 QR → 즉시 도장. 같은 부스 중복 불가. 서버가 QR 토큰과 도장 간격(90초)을 검사 — 번호만 쳐서 찍기·링크 연타 차단(5-4 참고).
- **오프라인 큐**: 전송 실패한 도장은 폰에 쌓았다가 온라인 복귀·30초 주기·부팅 시 재전송.
- **완주**: 7개(`CONFIG.stampGoal`) 모으면 축하 → 내 스탬프에 설문 카드. 설문 방식은 `surveyMode()` 로 셋 중 하나:
  - **앱 내 설문** (설문 링크 비어 있음, 기본): '설문 참여하고 교환권 받기' → `#/survey` → 문항 답하고 제출 → `visitor_survey_submit` RPC(서버가 완주 확인·응답 저장·`survey_done_at`) → 교환권. 완주 전·제출 후 재진입은 내 스탬프로 돌려보냄.
  - **외부 링크** (행사 설정에 구글폼 등 링크): '설문 참여하기'(새 탭, `surveyCodeField` 가 있으면 방문객 코드 미리 채움) + '설문 다 했어요'. 누른 시각을 폰에 저장(`wj:survey_open`)하고 `surveyMinSec`(기본 60초) 지나야 '다 했어요' 활성화(카운트다운 표시, 새로고침해도 유지). 구글폼 확인 메시지에 `주소?survey` 링크를 넣어두면 그 링크로 돌아온 폰은 자동 완료. 다른 탭에서 완료되면 원래 탭도 storage 이벤트·탭 복귀로 교환권으로 바뀜.
  - **없음** (링크도 문항도 없음): '다 했어요' 바로 가능.
  교환권(QR + 코드) → 운영본부에서 코드 확인 후 수령 처리.
- **홈 검색**: 이름·번호·학교로 검색, 분류 칩으로 필터.

### 지도 (사진 배치 기준)

- 운동장 스타디움(직선+반원), 가로 고정. 축구장 선·골대 그림.
- **트랙 바깥 한 줄** 1~73: 정문 오른쪽부터 시계방향으로 정문 왼쪽까지.
- **정문 좌우**: 오른쪽 운영본부 본1~3, 왼쪽 인생네컷 네1~2 (검정 타일).
- **골대 뒤편 두 열**: 왼쪽 74~77, 오른쪽 78~82. 골대 높이 기준 세로 가운데 정렬.
- **문 3개** 모두 위쪽: 정문(가운데, 트랙까지 점선 통로), 제1문(오른쪽), 제2문(왼쪽).
- 위쪽에 본부석·안내데스크(선물 교환), 아래 양끝 화장실.
- **분류 5개** (사진 범례 순서·색): 원리의정원 / 대담한갤러리 / 한바탕놀이터 / 상상공작소 / 상상그이상 (+ 운영·편의).
- **이름표**: 확대(전체 보기 대비 1.6배 이상)하면 화면 가운데에서 가까운 부스 3개만 흰 알약 이름표 + 칸까지 점선. 각 이름표는 자기 칸 바로 바깥에 두고 겹치는 만큼만 옆으로 밀어서(순서 유지) 겹침·점선 교차 없음. 위·아래 직선 구간만 두 줄, 곡선·좌우는 한 줄 계단식. 점선은 알약 아래 레이어라 글자 위를 안 지나감. 긴 이름은 두 줄로 접고 넘치면 …. 골대 뒤 열은 칸 옆 고정. 선택한 부스가 멀리 있으면 자기 칸 옆에 따로 표시. 드래그하면 따라 바뀜.
- **주변 부스 띠**: 같은 3개를 검색창 아래에 번호 + 전체 이름으로(도장 찍은 부스는 노랑). 긴 이름은 여기서 전부 읽힘. 누르면 그 부스로 이동 + 시트.
- **처음 열 때**: 폰(가로 700px 미만)은 정문 주변(본부·인생네컷·1~5번)으로 확대된 채 시작. PC는 전체 보기. 주소창 접힘 같은 세로 크기 변화에는 보던 자리 유지.
- **지도 검색**: 번호·이름이 하나로 좁혀지면 바로 그 부스로 확대 이동.
- **부스 탭 → 시트**: 번호·이름·기관·분류·구역, 도장 상태, [QR 찍고 도장] [정문에서 가는 길] [가까운 출구] [부스 소개].
- **정문에서 가는 길**: 정문 → 통로 링(짧은 방향) → 부스, 점선 애니메이션.
- **가까운 출구**: 부스에서 걷는 거리(통로 링 + 문까지)가 가장 짧은 문을 골라 경로 + 문 강조. 지도 우상단 출구 버튼은 "선택한 부스 → 없으면 마지막 도장 부스" 기준.

### 부스 소개

- 사진(있으면 상단), 체험 소개, 위치(구역 이름 + 지도 링크).
- **유튜브 영상**: URL 아무 형식(youtu.be, watch?v=, shorts, embed)이나 넣으면 임베드.
- **PDF 자료**: "부스 자료 PDF 보기" 버튼 → 새 탭.
- 도장 상태, [QR 찍고 도장 받기], [지도에서 위치 보기].

---

## 3. 관리자 기능 (`#/admin`)

| 탭 | 기능 |
|---|---|
| 현황 | 등록 방문객, 활동 방문객, 총 스탬프, 부스별·시간대별 집계, 완주자 |
| 선물 수령 | **교환권 QR 찍기(연속 처리)**: 학생 폰 교환권 QR(`WJ:uuid`)을 관리자 폰 카메라로 찍으면 학생 카드(이름·학교·도장 수·설문) → [수령 처리] → 1.2초 뒤 다음 학생 자동 대기. 완주 전·설문 전·이미 수령이면 이유만 보여주고 버튼 없음. 관리자 여러 명이 각자 폰으로 동시에 처리 가능(비번 로그인만 하면 됨, 기기 수 제한 없음). 그 외 코드·이름·학교 검색으로 수령 처리 / 취소. 학년·성별·설문 완료 여부 열 |
| 부스 | 카드 목록 편집(2026-09-16 표→카드 한 줄 2단, 가로 스크롤 없음: 1줄 번호·이름·기관·자료 칩·편집·삭제, 2줄 분류·구역·소개, 폰은 세로로 접힘 — 분류는 글자 입력: 있는 이름은 일부만 써도 인식, **없는 이름은 새 분류로 자동 추가**(색 자동, settings.categories 에 저장, 아무 부스도 안 쓰면 자동 정리, 홈 칩·지도 범례·대시보드에 반영). 구역은 드롭다운), 순서 이동(왼쪽 ≡ 끌어서 놓기 + [번호순 정렬], 2026-09-16, ↑↓ 화살표 제거), 추가·삭제, **엑셀 양식 내려받기**(xlsx: 열 너비·제목 줄 고정·소개 줄바꿈·'안내' 시트. ExcelJS 를 누를 때 CDN 에서 로드) · **엑셀·CSV 불러오기**(xlsx 는 '부스' 시트 2행부터, 번호 중복이면 거부, 통째로 교체) · CSV 내려받기(예전 방식), 최대 100개 |
| QR 시트 | 부스별 QR(토큰 포함) + 입장 QR 인쇄용. 인쇄 시 세로 A4 를 위아래로 반 → 가로로 놓는 반 장(210×148mm) 2개. QR 왼쪽(94mm, 분류 색 테두리, 일반 흑백 QR — 둥근 점 스타일은 만들었다가 사용자가 별로라 해서 뺌)·오른쪽에 분류 알약·BOOTH 번호 배지(분류 색, 최소 폭 44mm·줄바꿈 금지 — 한 자리·두 자리 같은 크기, 본1·네1 은 살짝 넓어짐)·부스명·기관·"QR 찍으면 도장 쾅" 안내·행사명. 왼쪽 색 띠 + 은은한 원 장식, print-color-adjust 로 배경색 인쇄. 가운데 점선 = 자르는 선, 2개마다 강제 쪽 나눔. 96장 → 48쪽. `@page margin:0` 으로 브라우저가 찍는 날짜·제목·주소·쪽번호 제거. 열면 서버에서 토큰 발급·조회. 카드 양식은 추후 조정 예정 |
| 행사 설정 | 행사명·일시·장소·완주 개수·설문 링크·설문 최소 시간·구글폼 코드 항목 ID·교환 마감·문의·긴급 공지·오늘의 일정·자료 링크. 서버 `settings`에 저장, 방문객 폰에 재실행/5분 내 반영 |
| 설문 | 앱 내 설문 문항 편집(표정 5단계·하나 고르기·여러 개·부스 고르기·자유 답변, 필수 여부, 순서, 최대 20개, 기본 7문항으로 되돌리기) → `settings.survey`. 응답 결과(문항별 집계·평균·주관식 목록) + 응답 CSV(이름 없이 학교·학년·학년군·성별 + 문항별 답) |
| 데이터 | CSV 내보내기(스탬프: +학년·성별 / 명단: +학년·학년군(초저·초고·중·고·성인)·성별·설문완료), 관리자 비번 변경, 부스 목록 기본(90+5)으로 되돌리기, 데모 데이터, 방문객 삭제 |

선물 수령 탭에 **방문객 찾기 · 수동 도장**(완주 전 포함 전체 검색, 코드 안내, 부스 번호로 도장 추가)도 있음.

### 부스 데이터 필드

| DB 컬럼 (booths) | 앱 안 필드 | 뜻 |
|---|---|---|
| `id` | `id` | `'b'+번호`. 도장 기록이 가리키는 키 |
| `number` | `n` | 번호. 숫자든 `본1`, `네2` 같은 문자든 됨 |
| `name` | `name` | 부스명 |
| `category` | `cat` | `sci` 원리의정원 · `craft` 대담한갤러리 · `play` 한바탕놀이터 · `app` 상상공작소 · `etc` 상상그이상 · `ops` 운영·편의 |
| `organization` | `org` | 운영기관 |
| `description` | `desc` | 소개 |
| `sort_order` | (배열 순서) | 표·지도 순서 |
| `zone` | `zone` | `''` 트랙 둘레 · `hq` 정문 오른쪽 운영본부 · `photo` 정문 왼쪽 인생네컷 · `goalL` 왼쪽 골대 뒤 · `goalR` 오른쪽 골대 뒤 |
| `video_url` | `video` | 유튜브 URL |
| `pdf_url`, `image_url` | `pdf`, `img` | 파일 URL 또는 업로드 결과 |

DB 컬럼은 2026-09-14에 풀네임으로 바꿈(SQL이 옛 컬럼을 감지해 rename). 앱 내부 필드는 짧은 이름 유지 — 화면 코드 수백 곳이 쓰고 사용자에겐 한글 라벨만 보이므로. 변환은 `SupabaseDB._b()`·`saveBooths()` 한 곳.

**같은 구역 안에서는 표 순서 = 지도 순서.** 트랙 둘레는 정문 오른쪽부터 시계방향.

### CSV (9열, UTF-8 BOM)

`번호, 부스명, 운영기관, 분류, 구역, 소개, 유튜브, PDF, 사진`
분류·구역은 위 표의 한글 이름으로. 모르는 값이면 분류는 상상그이상, 구역은 트랙 둘레로 들어감.

### 파일 업로드 (PDF·사진)

- 표의 **자료** 칸(▶영상·PDF·사진 상태 칩) 옆 **[편집]** → 시트에서 유튜브 주소(썸네일 미리보기)·PDF(파일 올리기/보기/지우기 또는 주소)·사진(파일 올리기/미리보기/지우기 또는 주소) 편집, [확인]으로 표에 넣음 (2026-09-16). 20MB 제한. 그 뒤 **[변경사항 저장]** 눌러야 반영.
- 로컬 모드: 브라우저 IndexedDB에 저장 → `local:경로`. **그 브라우저에서만 보임(목업용).**
- Supabase 모드: Storage 버킷 `booth-files`(public)에 올리고 공개 URL 저장. 버킷 생성 + anon 업로드 정책 필요.
- 유튜브는 링크만 (영상 파일 업로드 없음).

---

## 4. 데이터 레이어

`LocalDB` / `SupabaseDB` 같은 인터페이스: `booths, saveBooths, getVisitor, createVisitor, addStamp, stampsOf, allStamps, allVisitors, setGift, purgeVisitors, stats, seedDemo`.

### Supabase 설정 (`supabase_setup.sql`)

SQL Editor에 통째로 실행하면 끝(다시 실행해도 됨 — visitors에 grade·gender·survey_done_at 열을 `add column if not exists`로 붙이고 visitor_stats 뷰를 재생성). 테이블 `booths`·`visitors`·`stamps`·`admin_secret`(비번 해시)·`booth_tokens`·`settings`·`survey_answers` · 뷰 `booth_stats`·`hourly_stats`·`visitor_stats` · RLS · 관리자 RPC · 방문객 RPC · Storage 버킷 `booth-files`.
2026-09-15 추가분(9절 앱 내 설문 · 10절 운영 대시보드) 실서버 실행 완료. 실서버 검증: 완주 전 제출 NOT_DONE, 제출→교환권→survey_done_at, 재제출 무시, 관리자 결과 집계, 대시보드 풀 모드(집계·깔때기·학년·학교·티커·설문 평균) 전부 통과.

**권한 모델** (Auth 없음, anon 키 하나)

| 누가 | 할 수 있는 것 |
|---|---|
| anon 키 (방문객·관리자 화면 공통) | 테이블 직접 읽기는 `booths`·`settings`·집계 뷰(booth_stats·hourly_stats)만. 방문객 데이터는 RPC: `visitor_create`·`visitor_get(vid)`·`visitor_stamps(vid)`·`visitor_recover(code,name)`·`add_stamp(vid,bid,tok)`·`visitor_survey_done(vid)`. update·delete 불가 |
| 관리자 RPC (비번 동봉) | 쓰기: `admin_reset`·`admin_set_gift`·`admin_save_booths`(+토큰 발급)·`admin_save_settings`·`admin_add_stamp`·`admin_set_password`(8자 이상) · 읽기: `admin_login`·`admin_counts`·`admin_visitor_stats`·`admin_visitors`·`admin_stamps`·`admin_find_visitor`·`admin_booth_tokens`·`admin_survey_answers`·`admin_dashboard`·`admin_booth_visitors`(부스별 참여자 명단) |
| 방문객 설문 RPC | `visitor_survey_submit(vid, answers)`: 완주 전이면 NOT_DONE, 8KB 초과 TOO_LONG, 두 번 제출은 처음 것 유지. `survey_answers` 는 정책 없음(RPC 로만) |

- 비번은 pgcrypto `crypt`로 해시해 `admin_secret`에 저장. 초기값 1234 → 관리자 › 데이터에서 변경(서버에 저장됨). 로그인 10분 안 10회 틀리면 1분 잠금(`admin_login_fail` 표, 5-11 참고). 지연(pg_sleep)은 없음.
- 로그인 화면은 그대로(비번 한 칸). Supabase 모드면 `admin_login` RPC로 검사 → 통과한 비번을 `sessionStorage`에 두고 관리자 RPC마다 같이 보냄. 탭 닫으면 로그아웃. 다른 데서 비번이 바뀌면 다음 관리자 동작에서 자동 로그아웃.
- 방문객 이름·학교는 anon 키로 못 읽음(2026-09-14). 관리자 화면도 비번 RPC로만 읽음. 행사 후 초기화(개인정보 파기)는 데이터 탭.
- PostgREST 1000행 제한 → 명단·스탬프 내보내기와 달성자 목록은 Range 헤더로 1000개씩 페이징.
- Storage 업로드는 anon insert 허용(관리자 화면도 anon 키라서). 덮어쓰기·삭제는 불가, 파일명에 시각이 붙어 안 겹침.
- 연결 순서: ① 프로젝트 만들기 → ② SQL 실행 → ③ `CONFIG.supabaseUrl`·`supabaseAnonKey` 채우기 → ④ 관리자 › 데이터에서 비번 변경.

---

## 5. 이번 세션에서 바뀐 것 (2026-09-07)

1. 부스 상한 100개 대응 (`boothMax`), 88개 기준 시드.
2. 지도를 실제 배치도 사진대로 재구성 (트랙 바깥 한 줄 + 정문 좌우 + 골대 뒤 두 열 + 문 3개 + 축구장).
3. 분류를 사진 범례(원리의정원 등 5개 + 운영·편의)로 교체.
4. 부스 `zone` 필드 추가, 관리자 표·CSV에 구역 열.
5. 가까운 출구 찾기 (부스 시트 버튼 + 지도 출구 버튼).
6. 부스 소개에 유튜브·PDF·사진 필드, 관리자 표·CSV 반영.
7. PDF·사진 파일 업로드 (로컬 IndexedDB / Supabase Storage).
8. 지도 이름표: 확대 시 가까운 6개만, 선반식 배치 + 점선 연결(겹침·교차 없음). 주변 부스 띠(전체 이름).
9. 골대 뒤 열 세로 가운데 정렬, 이름표는 바깥쪽으로.
10. 지도 검색에서 하나로 좁혀지면 자동 이동.
11. 관리자 표 분류·구역 셀렉트 폭 깨짐 수정.
12. 한글 부스 번호(본1 등) 라우트·QR 대응.
13. 버그: CSV 마지막 행이 빈 칸으로 끝나면 빠지던 것.
14. 폰에서 지도 열면 정문 근처로 확대 시작. 세로 리사이즈에 위치 유지.
15. 시드를 부스배치도 PDF의 실제 82개 부스로 교체, 행사 날짜(10/10) 홈·안내에 표시.

## 5-1. 2026-09-08

1. `supabase_setup.sql` 작성: 테이블·뷰·인덱스·RLS(anon은 insert/select만)·관리자 RPC·Storage 한 번에.
2. 관리자 쓰기 3개(초기화·선물지급·부스저장)를 비번 동봉 RPC로. anon 키로는 update·delete 전부 불가.
3. 관리자 비번을 서버에서 검사(`admin_login`), 변경도 서버 저장(`admin_set_password`). 화면은 그대로.
4. 대량 조회 1000행 페이징, 빈 본문(204) 처리, 새로고침 세션 유지, 로그아웃 버튼.
5. 가짜 Supabase 서버로 등록→도장→중복→재접속→관리자 로그인(오답 포함)→집계→선물→부스 저장→비번 변경→초기화→anon 직접 삭제 차단 확인.

---

## 5-2. 2026-09-09 (주최측 피드백 반영)

1. 완주 기준 5 → 7 (`CONFIG.stampGoal`).
2. 완주 후 흐름: 설문 참여 → '설문 다 했어요'(자기신고) → 교환권 → 운영본부 수령. `CONFIG.surveyUrl` 자리(아직 빈 값). 문구 전부 '안내데스크' → '운영본부'.
3. 등록: 학교·구분·학년·이름·성별. 성인은 소속 선택. visitors에 `grade`·`gender`·`survey_done_at`.
4. 관리자 선물 탭에 학년·성별·설문 열, 명단 CSV에 학년군·설문완료 열.
5. 부스 시드 83~90 추가 자리 8개 (주최측 '텐트 90개').
6. `CONFIG.testStampInput`(현재 true): [QR 찍기]가 카메라 대신 부스 번호 입력창. https 없는 로컬 테스트용. **배포 전 false**.
7. 주최측 질문 답변용: 부스체험 확인 = 부스에 붙인 QR을 학생 폰으로 찍으면 자동 도장(부스 측 조작 없음). 부스 설명은 관리자 › 부스 CSV로 넣거나 표 파일 받아서 넣으면 됨.

## 5-3. 2026-09-14 (Supabase 실제 연결)

1. 계정으로 Supabase 프로젝트 생성, SQL 실행, `CONFIG.supabaseUrl`·`supabaseAnonKey` 채움 (publishable 키 `sb_publishable_…` = anon 키. 공개용).
2. SQL 수정 2건 (실제 Supabase에서 걸림):
   - pgcrypto가 `extensions` 스키마에 있어서 함수 안 `crypt`를 못 찾음 → 모든 RPC `set search_path = public, extensions`.
   - Supabase가 WHERE 없는 DELETE를 막음(safeupdate) → `admin_reset`·`admin_save_booths`의 delete에 `where true`.
3. 실서버 검증 통과: REST/RPC(로그인·오답·부스저장·등록·도장·중복 409·설문 1회·선물·집계·anon PATCH/DELETE 0행·비번 변경·초기화) + 브라우저(playwright, 폰 등록→7개 도장→설문→교환권→새로고침 유지 / PC 관리자 로그인→현황→선물 수령 처리). 콘솔 에러 0. Storage 업로드·공개 읽기 OK.
4. 테스트 데이터는 `admin_reset`으로 지움. Storage에 `test/hello.txt` 하나 남아 있음(anon 삭제 불가, 대시보드에서 지우면 됨).
5. 부스 테이블은 비어 있음 → 앱이 `SEED_BOOTHS`로 표시. 관리자 › 부스에서 한 번 저장하면 서버에 올라감.

6. 부하 테스트 (`부하테스트_2026-09-14.png` / `.json`): 동접 50→2000명이 3초 안에 동시 등록+도장7+조회, 현황 화면 2초 폴링. 무료 플랜 기준 **800명 폭주까지 에러 0·p95 1초 이내(698 req/s)**, 1200명은 에러 0이지만 p95 15초, 2000명은 66% 타임아웃. 실제 행사 부하(평균 2~3 req/s, 피크 수십)는 이보다 훨씬 낮음. 불안하면 행사 당일만 Pro(컴퓨트 Small) 또는 현황 폴링 간격 늘리기. 테스트 후 admin_reset으로 정리.

## 5-4. 2026-09-14 (치팅 방지·타임아웃·관리자·행사 설정)

1. **타임아웃**: `SupabaseDB._q`에 AbortController. 방문객 요청 8초(`CONFIG.requestTimeoutMs`), 관리자 20초(`adminTimeoutMs`). 넘기면 throw → 도장은 pending(폰 저장 후 30초마다 자동 재전송). 재전송에서 dup(서버엔 이미 들어감)이면 서버 목록으로 맞춤.
2. **치팅 방지** (QR 주소 `?b=7&t=토큰`):
   - 서버 `booth_tokens`(anon 못 읽음). 부스 저장 때 자동 발급, 기존 토큰 유지(인쇄한 QR 안 깨짐). 관리자 › QR 시트가 `admin_booth_tokens`로 받아 QR에 넣음(부스가 서버에 없으면 먼저 저장).
   - 도장은 `add_stamp(vid,bid,tok)` RPC로만. anon 직접 insert 차단. 순서: 이미 찍음→dup / 토큰 불일치→BAD_TOKEN / 마지막 도장 후 `stamp_gap_sec()`(90초) 안→TOO_FAST:남은초 / 삽입. (BAD_TOKEN 0.3초 지연은 5-11 에서 제거)
   - 클라이언트 `qrTok`(sessionStorage): 딥링크·스캐너에서 토큰 저장, 등록 갔다 와도 유지. 거절 화면 "부스 앞 QR을 찍어주세요" / "N초 후에 찍을 수 있어요". 거절은 pending에 안 넣음.
   - `CONFIG.stampGapSec`(90)은 로컬 모드 검사·문구용. 서버 값은 SQL `stamp_gap_sec()` 숫자 — 둘 같게.
   - 테스트 모드(번호 입력): 같은 탭에서 관리자 › QR 시트 한 번 열면 토큰이 세션에 들어와 번호만으로 통과. 아니면 QR 주소 전체 붙여넣기.
   - 한계: 인쇄된 고정 QR이라 "친구가 링크 카톡으로 보내주기"는 원천 차단 불가. 시간 간격으로 이득을 없앤 것.
3. **관리자 진입**: 홈 톱니 버튼 제거. `#/admin` 직접 입력(`CONFIG.adminPath`). 새 비번 8자 이상(서버·화면).
4. **행사 설정 탭**(관리자 › 행사 설정): 행사명·일시·장소·완주 개수·설문 링크·선물 교환 마감·문의 안내·오늘의 일정·자료 링크. 서버 `settings` 테이블(jsonb 1행, anon 읽기, `admin_save_settings`로 쓰기), 로컬은 `wj:settings`. 부팅 때 + 5분마다 `loadSettings()` → CONFIG·SCHEDULE·RESOURCES 덮음. 완주 개수 바꾸면 진행 중인 사람에게도 적용.
5. 실서버 검증: 번호만/가짜 토큰 거절, 진짜 QR 도장, 연속 스캔 거절(남은 초 표시), 재스캔 dup, 설정 저장→방문객 반영, anon 직접 insert 차단(RLS), booth_tokens 읽기 빈 배열.

## 5-5. 2026-09-14 (개인정보 잠금·이어받기·수동 도장·공지·라이브러리)

1. **개인정보 잠금**: anon 키로 visitors/stamps/visitor_stats 를 읽던 정책 전부 제거(뷰는 `revoke`). 방문객은 RPC `visitor_create`·`visitor_get(vid)`·`visitor_stamps(vid)`(uuid 아는 사람 = 본인), 관리자는 비번 RPC `admin_counts`·`admin_visitor_stats(min_n,off,lim)`·`admin_visitors`·`admin_stamps`(1000행 페이징)·`admin_find_visitor(q)`. booth_stats·hourly_stats 는 개인정보 없는 집계라 그대로. 확인: anon `visitors?select=*` → `[]`, `visitor_stats` → 401.
2. **기록 이어받기**: 내 스탬프 화면 상단에 6자리 코드 항상 표시. 등록 화면 아래 '기록 이어받기' → 코드+이름 → `visitor_recover` → 새 폰에 `wj:me` 저장. 카톡 인앱 브라우저↔사파리, 폰 교체, 시크릿 모드 케이스용. 코드 모르면 운영본부가 이름으로 찾아 알려줌.
3. **수동 도장**: 관리자 › 선물 수령 탭에 '방문객 찾기 · 수동 도장' 카드. 이름·학교·코드로 전체 방문객 검색 → [도장 추가] → 부스 번호 입력 → `admin_add_stamp`(토큰·간격 검사 없음). 중복이면 안내.
4. **긴급 공지**: 행사 설정에 '긴급 공지' 칸(`settings.notice`). 비어 있지 않으면 홈 맨 위 빨간 띠. 5분 폴링으로 열린 폰에도 반영.
5. **라이브러리 로컬**: `qrcode.min.js`·`jsQR.js` 를 HTML 옆에 둠(로컬 먼저, 없으면 CDN). cdnjs 의 jsQR 1.4.0 경로가 404 였음 → 폴백을 jsdelivr 로 교체. **배포할 때 이 두 파일도 같이 올릴 것.**
6. 실서버 검증: 등록→도장→새로고침 재접속→다른 브라우저에서 이어받기(이름 틀리면 거절)→관리자 찾기→수동 도장→중복 안내→새 폰에 반영, 공지 저장→홈 표시, 명단 RPC 조회. 콘솔 에러 0.

## 5-6. 2026-09-14 (DB 컬럼명·소스 주석)

1. `booths` 컬럼 풀네임: n→number, cat→category, org→organization, desc→description, ord→sort_order, video→video_url, pdf→pdf_url, img→image_url. SQL 에 `do $$` 블록으로 옛 이름 감지 시 rename. `admin_save_booths` 새 이름. 앱은 `_b()` 매핑.
2. 소스 주석: 스크립트 맨 앞에 **설계 개요**(파일 구조·데이터 흐름·로컬 저장 키·서버 권한), DATA LAYER 메서드 표, 함수별 역할 주석(부팅·라우터·화면·스캐너·관리자 탭), BOOT 순서.

## 5-7. 2026-09-15 (v3 폴더 — 설문 강화·앱 내 설문·운영 대시보드·지속 테스트)

폴더 `stamp-rally-v3` 로 복사해서 작업. 원본 v2 는 안 건드림. 테스트 스크립트는 `tests/`(playwright, node_modules 는 정션).

1. **설문 자기신고 보강** (외부 링크 모드): `surveyMinSec`(기본 60초) 카운트다운 후 '다 했어요' 활성화, 누른 시각 폰 저장(새로고침 유지). `surveyCodeField`(구글폼 entry.xxx) 로 응답에 방문객 코드 자동 입력. 확인 메시지 `주소?survey` 복귀 링크로 자동 완료(완주 전이면 무시). 다른 탭 완료 → 원래 탭 동기화(storage·visibilitychange). 행사 설정 탭에 두 칸 + 안내 문구 추가.
2. **앱 내 설문** (기본 모드): `#/survey`, 기본 7문항(설문_목업.html 기준: 표정 5단계·부스 고르기(내 도장 칩)·복수·단일 3개·자유). 진행률·필수 검사·제출 → `visitor_survey_submit`. 관리자 › **설문 탭**: 문항 편집·저장(`settings.survey`, 다른 설정 키 보존), 결과 집계(평균·분포·부스 TOP·주관식 목록), 응답 CSV. `SURVEY` 는 5분 폴링으로 방문객 폰에 반영.
3. **운영 대시보드** `운영대시보드.html`(→ `/dashboard`): 관리자 비번 로그인, 15초 자동 갱신(10초~수동), KPI 7개(등록·활동·도장·완주·설문·수령·수령 대기 + 최근 15분 증가), 배치도 히트맵(최근 15분/누적 토글, 스탬프앱과 같은 좌표), 10분 단위 추이·시간대별, 붐비는 부스 TOP10, 완주 깔때기, 학년군·성별, 학교 TOP10, 최근 도장 티커, 설문 만족도(척도 평균), 부스 전체 표(95개, 열 제목 클릭 정렬 + 오름/내림 토글·검색, 30분 넘게 조용하면 마지막 시각 빨강 — QR 점검용). 카드 안 목록은 길어지면 카드 안에서 스크롤(페이지가 안 길어짐). '조용한 부스' 카드는 사용자 요청으로 뺌(전체 표의 빨간 시각이 대신). 데이터는 `admin_dashboard` RPC 하나(집계만, 이름 없음). RPC 없으면 기본 모드로 동작.
4. **관리자 탭 경합 수정**: 집계 기다리는 사이 다른 탭으로 옮기면 현황이 덮어쓰던 것.
5. **테스트** (`tests/test_v3.mjs` 실서버, `tests/test_survey.mjs` 로컬 모드): A안 타이머·코드 미리 채움·새로고침 유지·B안 복귀·탭 동기화·행사 설정 저장/검증·대시보드 로그인/집계/지도 95개/로그아웃·앱 내 설문 제출/재진입 차단/편집기/결과/CSV 전부 통과, 콘솔 에러 0.
6. **지속 테스트** (`tests/soak.mjs`, 결과 `지속테스트_2026-09-15.png/.json/.html`): 무료 플랜, 실제 앱과 같은 RPC(visitor_create → add_stamp 토큰·90초 간격 → 앱 다시 열기 15%), 대시보드 15초 폴링 동시. **초당 4명 도착 × 10분 = 방문객 2,344명, 요청 11,558건(평균 19/초, 최대 32/초), 에러 0, p50 53ms · p95 64ms · p99 97ms, 지연 드리프트 없음(첫 분 p95 90 → 마지막 64), 대시보드 폴링 p50 101ms.** 행사 실부하(초당 2~3명 등록)보다 높은 부하를 10분 버팀. 폭주 테스트(09-14, 800명 동시까지 에러 0)와 성격이 다름 — 그쪽은 순간 최대치. 결론: 무료 플랜으로 행사 시간 내내 OK, 위험은 개막 직후 순간 폭주뿐.
7. `배포.ps1`: `_3.html` → `deploy/index.html`, 대시보드 → `deploy/dashboard.html`. 2026-09-15 Vercel 첫 배포(https://your-app.vercel.app), 실기기 카메라 QR 확인됨.
8. 지도 드래그 경계(`clampView`): 화면 밖으로 끝없이 밀리던 것 → 보이는 영역에 지도가 최소 40% 남도록 잠금. 실기기 피드백.

## 6. 아직 안 한 것

- v2 그대로 보낼지 v3 쓸지 결정 (v3 = v2 + 위 5-7)

- **관리자 비번 1234 → 변경** (관리자 › 데이터). 배포 전 필수
- 설문 링크 받으면 관리자 › 행사 설정에 넣기 (CONFIG.surveyUrl 는 기본값)
- 부스 설명 표 받으면 관리자 › 부스 › **엑셀 양식 내려받기** → 엑셀에서 채우기 → **엑셀·CSV 불러오기** (83~90 자리·추후 입력 9개 포함). 불러오면 바로 서버 저장됨
- 자료 링크(리플릿) example.com → 관리자 › 행사 설정에서 교체 (만족도조사 줄은 앱 내 설문으로 대체돼 2026-09-16 삭제)
- 부스 목록: PDF(8/24) 기준으로 시드 넣음. 추후 입력 자리 9개(20~23, 42, 74~77) · 최종본 나오면 CSV로 교체
- QR 시트는 배포 주소(your-app.vercel.app)로 관리자 › QR 시트에서 뽑아 인쇄. 도메인 바꾸면 재인쇄
- 카메라 QR 실기기 테스트 (배포됐으니 폰으로 접속해서)

## 5-8. 2026-09-15 저녁 (무료 한도 대비 — 요청 수·전송량 줄이기)

외부 조언(Vercel Edge Requests 100만/월, Supabase egress 5GB, Pro 는 월 $25·컴퓨트 수동 변경) 반영.
1. **js 1년 immutable 캐시 + 파일명 버전**: `deploy/vercel.json` 에서 `*.js` 는 `public, max-age=31536000, immutable`, HTML 은 `no-cache`. HTML 이 `qrcode.min.js?v=배포시각`·`jsQR.js?v=…` 를 가리키고, `배포.ps1` 이 `?v=DEV` 를 배포 시각으로 치환 → 코드 고쳐 배포하면 HTML 만 새로 받고 새 js 를 자연히 받음. 재방문 Vercel 요청 = HTML 1건.
2. **favicon 인라인**(data URI) → 404 요청 없음.
3. **부스 목록 캐시**: `settings.boothsVersion`(관리자가 부스 저장할 때 `SupabaseDB.saveBooths` 가 올림) ↔ 폰 `wj:booths_cache`. 버전 같으면 booths 요청 생략. 부팅 순서 설정 → 부스. 5분 폴링에서 버전 바뀌면 다시 받음. 리로드당 Supabase 요청: 미등록 1개(설정), 등록자 3개(설정·내 정보·내 도장).
4. 실측(2026-09-15 배포본, Chrome): 첫 방문 Vercel 2 + 스캐너 열 때 jsQR 1 / 재방문 Vercel HTML 1(js 는 디스크 캐시) · Supabase 설정 1 · 4xx 없음.
5. **Pro 판단**: 1만 명이면 무료로 충분. 5만 명 상단 대비면 행사 2~3일 전 Pro(월 $25) + 컴퓨트 Micro 수동 변경(2분 다운타임). 무료 프로젝트는 7일 미사용 시 일시정지되니 행사 전주에 앱 한 번 열기. 행사 후 CSV export 필수(무료는 백업 없음).

## 5-9. 2026-09-15 밤 (관리자 교환권 QR 스캔)

스캐너(`openScanner`)를 범용화 — `opts.onCode` 를 주면 부스 파싱 대신 콜백. 관리자 › 선물 수령 [교환권 QR 찍기]가 이걸로 연속 처리. 교환권 QR 내용은 그대로 `WJ:uuid`(링크 아님 → 다른 사람이 폰 카메라로 찍어도 글자만 뜨고 아무 일도 없음, 수령 처리는 관리자 로그인 화면에서만). 테스트 모드에선 코드 입력칸. e2e `tests/test_gift_scan.mjs` 11건 통과.

## 5-10. 2026-09-15 밤 (4만 명 대비 2차 — CDN 우선·부스 시드)

1. **라이브러리 로더 `loadLib`**: jsQR·qrcode 를 필요할 때만, jsdelivr CDN 먼저 → 3초 안에 안 오거나 실패하면 같은 서버 파일(`?v=배포시각`). 정적 `<script>` 태그 제거. 교환권 QR·QR 시트는 `loadQRCode()` 뒤에 그림. 효과: Vercel 첫 방문 요청 3건 → 1건(HTML). 단점: CDN 이 느리면 최대 3초 대기 후 폴백.
2. **부스 시드 버전 매칭**: `CONFIG.seedVersion` 에 서버 `settings.boothsVersion` 을 배포 스크립트가 박음. 앱은 설정(1KB)만 읽고 버전이 같으면 HTML 안 `SEED_BOOTHS`(desc·video·pdf·img 포함) 사용 → 부스 요청 0. 관리자가 부스를 고치면 버전이 달라져 서버에서 받음(정상 동작), 재배포하면 다시 절약. `배포.ps1` 이 REST 로 부스·버전을 읽어 치환, 실패하면 경고만 하고 그대로 배포(seedVersion 0 = 항상 서버).
3. 실측(배포 후): 첫 방문 Vercel 1(HTML) + Supabase 1(설정), 라이브러리는 jsdelivr. 4만 명 추정: Vercel 요청 40만/100만, Supabase 전송 ≈1.5GB(원본 기준)/5GB.
4. 테스트 `tests/test_seed.mjs` 9건(시드 매칭·CDN 로드·CDN 차단 폴백·CDN 무응답 3초 폴백·교환권 지연 로드).

## 5-11. 2026-09-15 밤 (외부 조언 2차 — 비번 잠금·지속 테스트 2차·부스 참여자 명단)

1. **`pg_sleep` 전부 제거, 실패 횟수 잠금** (SQL 11절): 틀린 비번 0.5초·틀린 토큰/이어받기 0.3초 지연은 DB 연결을 붙들어서 일부러 퍼부으면 연결 풀(무료 60개)이 먼저 바닥나는 자폭 스위치였음. 대신 `admin_login_fail` 표에 로그인 실패를 세어 **10분 안 10회 → 1분 잠금**(잠긴 동안은 해시 계산 없이 `ADMIN_LOCKED:남은초` 즉시 거절, 맞는 비번도 거절, 모든 관리자 RPC 공통). 로그인이 아닌 관리자 RPC 에 틀린 비번은 즉시 거절만 하고 횟수는 못 셈(예외로 끝나는 함수는 update 도 같이 롤백됨) — 8자 이상 비번이면 무차별 대입은 어차피 불가. 앱·대시보드는 잠김 문구 표시, 대시보드는 다음 갱신에 자동 재시도. 강제 해제 SQL 은 11절 주석. 테스트 `tests/test_lockout.mjs`(실서버, 11절 적용 후 `node test_lockout.mjs <비번>` — 1분 잠김을 실제로 만들고 풀리는 것까지 확인).
2. **부스별 참여자 명단** (SQL 12절 `admin_booth_visitors(pw,bid,off,lim)`): 관리자 › 현황 부스 표 줄을 누르면 시트에 그 부스 도장 명단(시각·이름·학교·학년·성별·그 사람 도장 수, 최근순, 이름·학교 검색). 운영 대시보드는 부스 전체 표 줄 클릭 → 아래 카드, 15초 갱신 때 같이 새로 읽음. 로컬 모드도 됨. 테스트 `tests/test_booth_visitors.mjs` 11건.
3. **지속 테스트 2차** (`tests/soak.mjs 15 6 부하`, 결과 `지속테스트_2026-09-15_15분_6ps.json`): 외부 조언대로 서로 다른 uuid + 유효 토큰, **초당 6명 도착 × 15분**, 방문객마다 92~122초마다 도장(운영 대시보드 2대 `admin_dashboard` 15초 폴링 동시). 방문객 5,242명 · 요청 35,314건 · **에러 0** · 전체 p50 53ms · p95 73ms · p99 2.3s. 분당 부하는 계속 올라가 13분째 초당 59건(p95 65ms)까지는 평평했고, **마지막 14분째 초당 65건에서 쓰기(등록·도장·설문) p95 가 3~4초, 최대 6초로 튐**(읽기 visitor_get/visitor_stamps 는 p95 249ms, 대시보드 RPC 최대 4.6초). 에러·타임아웃은 없었고 요청이 줄만 섰음. 6분째(초당 37건)에도 잠깐 p95 267ms 튐이 있었음. 결론: **무료(Nano) 컴퓨트의 한계는 대략 초당 60건 쓰기**. 실제 행사는 방문객이 부스마다 5~10분 머물러서 동시 5,000명이어도 도장은 초당 10~15건 수준이라 이 테스트가 4~5배 가혹함 → 무료로 가능하다고 보되, 개막 폭주·안전 보험으로 Pro+Micro 는 여전히 선택지. 테스트 데이터(이름 부하0~부하5241, 도장 약 3만)는 남겨둠 — 사용자가 지움.
4. **대시보드 차트 숫자 정리**: 도장 추이(10분 칸 37개)에 값 숫자가 4칸마다 붙어 6573 같은 수가 겹쳐 안 읽힘 → `barChart` 를 왼쪽 눈금(1·2·5 단위 자동, 천 단위 콤마, 1만 이상은 `1.2만`) + 값 숫자는 **최고점·현재 칸만**(막대 위에서도 읽히게 테두리) + 막대마다 툴팁(시각 범위·개수)으로. 시간대별 차트는 값 전부 콤마.
5. **관리자 › 현황 부스별 표 정렬**: 열 제목(부스 번호·운영·스탬프·최근) 클릭, 한 번 더 누르면 오름·내림. 번호는 숫자 → 문자(본1·네1) 순. 탭을 오가도 정렬 유지. 테스트 `tests/test_admin_sort_chart.mjs` 12건.
